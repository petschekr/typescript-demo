let variable: boolean = true;

let variable2 = "Hello, world!";

let arr: string[] = ["", "string 2"];

let tuple: [string, number] = ["Hello", 2];

enum DaysOfWeek {
    Monday, Wednesday, Friday
}

let variable3: DaysOfWeek = DaysOfWeek.Monday;

let anyvariable: any = 2;

let variable4: number | null = null;

function doesSomething(): void {

}
let func = () => {
    while (true) {}
}

let obj = {
    "hello": 2
}

interface MyInterface {
    hello: number;
    myString: string;
}

let obj2: MyInterface = {
    hello: 2,
    myString: ""
}